name: Validate submissions

# Runs on every pull request, including from forks. No secrets are used and nothing is signed, which
# is deliberate: this is the review gate (design doc §5.4), and a gate that only works for
# maintainers is not a gate.
on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout registry
        uses: actions/checkout@v4

      # The validator IS the client's validator (electron/plugins/manifest.mjs). Vendoring a copy
      # here would let the two drift, and then "strict mode passed" would stop meaning "clients will
      # accept this" — which is the entire value of validating at review time.
      # Which app version? Not `main` — see _app-checkout.md. Validating against unreleased code
      # would accept manifests that every shipped client skips, with a green check to say otherwise.
      - name: Resolve app ref
        id: app
        env:
          APP_REF: ${{ vars.APP_REF }}
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ -n "$APP_REF" ]; then
            echo "ref=$APP_REF" >> "$GITHUB_OUTPUT"
            echo "pinned to $APP_REF via the APP_REF variable"
            exit 0
          fi
          ref="$(gh release view --repo zeraix/zeraix --json tagName --jq .tagName 2>/dev/null || true)"
          if [ -z "$ref" ]; then
            echo "::error::could not resolve the latest zeraix/zeraix release. Set the APP_REF variable to a tag."
            exit 1
          fi
          echo "ref=$ref" >> "$GITHUB_OUTPUT"
          echo "using the latest app release: $ref"

      - name: Checkout app (for the shared validator)
        uses: actions/checkout@v4
        with:
          repository: zeraix/zeraix
          ref: ${{ steps.app.outputs.ref }}
          path: .app

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      # A missing builder means the app release predates the plugin tooling — a MODULE_NOT_FOUND
      # stack trace does not say that, and this is the first thing anyone will hit.
      - name: Check the shared tooling is present
        run: |
          if [ ! -f .app/scripts/build-registry-index.mjs ]; then
            echo "::error::${{ steps.app.outputs.ref }} of zeraix/zeraix has no scripts/build-registry-index.mjs."
            echo "::error::That release predates the plugin tooling. Ship a release containing it, or set"
            echo "::error::the APP_REF variable to a ref that has it."
            exit 1
          fi

      # The generated built-in skills are not present here — publish.yml materializes those from the
      # app's src/skills. Hand-authored official plugins (plugins/zeraix/office-suite) ARE committed
      # and do appear.
      #
      # Which is why the reserved-publisher gate is keyed on where the pull request came from: a fork
      # may not claim "zeraix", and a branch on this repo is a maintainer publishing an official
      # plugin and may. A fork cannot influence this value — it is computed by Actions, not read from
      # the submission.
      - name: Validate manifests
        env:
          FROM_FORK: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
        run: |
          if [ "$FROM_FORK" = "true" ]; then
            node .app/scripts/build-registry-index.mjs --root . --check
          else
            node .app/scripts/build-registry-index.mjs --root . --check --allow-reserved
          fi
